source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rack-reprocess.gemspec
gemspec

gem 'rack'

group :development do
  gem 'rack-test'
end
